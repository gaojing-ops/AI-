# 工具使用说明

## 开新书

运行 `python gui_app.py` 后，先用“开书向导”生成项目配置和空白模板。逐章细纲是托管质量的核心，建议先写完整再开始一键托管。

## 配置文件

- `project_config.json`：总章数、卷范围、字数、托管策略。
- `plot/tool_rules.json`：禁词、旧名、跨章事件、章节门控。
- `plot/reveal_rules.json`：真相揭示节奏。
- `prompts.json`：RAG 设定提取模板。

## 输出

- `output/` 保存工作稿。
- `publish/` 只保存“正式可用”的章节。
- `logs/chapter_status.jsonl` 记录每章状态。
- `logs/batch_audit.jsonl` 记录托管过程。

## 题材模板

`templates/` 下有校园读心、末世悬疑、修仙升级、年代文、霸总豪门、无限流。复制其中的 `prompts.json`、`tool_rules.json`、`reveal_rules.json` 到项目根目录或 `plot/` 对应位置后再细改。

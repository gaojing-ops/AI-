# 小说工具_一键托管交接文档

更新时间：2026-05-18  
本地目录：`D:\小说工具_一键托管`  
GitHub 仓库：`https://github.com/gaojing-ops/AI-`  
当前主提交：`2012d36`

## 1. 项目定位

这是一个通用小说一键托管工具底座，用来把“提供大纲、卷纲、逐章细纲后，自动从下一章写到终章”的流程做成可复用工具。

核心目标：

- 不绑定旧书，不内置旧角色、旧剧情、旧禁词。
- 新书通过配置、模板、人物设定、世界观、大纲和细纲驱动。
- 托管主链路只做生成、硬伤检查、状态记录、记忆维护。
- 普通问题记录日志后继续跑；真正会污染整本书的问题才暂停。
- 最终把工作稿、待审稿、发布稿、日志分层保存。

## 2. 交付内容

本工具包含：

- Python 源码版 GUI。
- 通用空白新书模板。
- 题材模板包：校园读心、末世悬疑、修仙升级、年代文、霸总豪门、无限流。
- 项目级配置、提示词、规则、真相揭示规则。
- 开书向导、体检大纲、一键写完整本、断点续跑、发布包导出。
- 跨章扫描、重复结构扫描、正式稿保存前检查、硬伤门、细纲执行清单。

不包含：

- 旧项目正文。
- 旧项目角色设定。
- 真实 API Key。
- 本地生成稿、日志、历史缓存。

## 3. 快速启动

1. 解压分享包。
2. 进入工具目录。
3. 安装依赖：

```powershell
pip install -r requirements.txt
```

4. 启动 GUI：

```powershell
python gui_app.py
```

5. 首次使用时：

- 点击“设置”，填写 API Key。
- 点击“开书向导”，填写书名、目标总章数、卷范围和题材模板。
- 在 `characters/`、`world_building/`、`plot/` 里补齐新书资料。
- 点击“体检大纲”，先把 FAIL 项处理掉。
- 点击“一键写完整本”，开始托管。

## 4. 目录说明

- `gui_app.py`：主界面，负责按钮、开书向导、体检大纲、一键托管、导出。
- `generator.py`：生成主流程，负责调用模型、保存章节、检查硬伤、维护记忆。
- `cross_chapter_scanner.py`：跨章事件、禁词、门控、旧名和重复结构扫描。
- `skill_engine.py`：技能审稿和多维检查。
- `rag_engine.py`：上下文检索和资料加载。
- `de_ai_flavor.py`：独立润色工具，不进入托管主链路。
- `scan.py`：文本扫描辅助工具。
- `make_cover.py`：封面生成辅助工具。
- `requirements.txt`：Python 依赖。

资料目录：

- `characters/`：角色设定。
- `world_building/`：世界观设定。
- `plot/`：大纲、卷纲、逐章细纲、规则表。
- `skills/`：审稿技能配置。
- `templates/`：题材模板。
- `output/`：工作稿、待审稿、托管检查稿。
- `publish/`：通过发布检查的干净章节。
- `logs/`：托管状态、审计日志、阶段审稿摘要。
- `history/`：历史缓存和断点数据。

## 5. 关键配置

### `config.json`

本地私有配置，主要放 API Key、模型、温度、作者名。分享和上传时不要带真实 Key。

可参考 `config.example.json`：

- `api_key`
- `base_url`
- `model`
- `temperature`
- `max_tokens`
- `author_name`

### `project_config.json`

新书项目配置：

- `book_title`：书名。
- `genre_template`：题材模板名。
- `target_total_chapters`：目标总章数。
- `volume_ranges`：卷范围。
- `chapter_char_min`：最低字数。
- `chapter_char_target_min` / `chapter_char_target_max`：目标字数区间。
- `trusteeship_policy`：默认 `lenient_continue`。
- `auto_outline_from_volume`：缺逐章细纲时是否从卷纲兜底生成。
- `health_check_future_chapters`：体检向后检查多少章。
- `tone_rules`：额外文风要求。

### `prompts.json`

托管写作和审稿提示词。通用要求放这里，不要在代码里写死题材或角色。

### `plot/tool_rules.json`

项目规则入口：

- 禁词。
- 旧名。
- 跨章事件。
- 章节门控。
- 记忆保护词。
- 平台敏感词。

### `plot/reveal_rules.json`

真相揭示节奏入口：

- 真相主题。
- 章节揭示层级。
- 语义触发词。
- 严格模式。

## 6. 一键托管流程

“一键写完整本”大致流程：

1. 读取 `project_config.json` 的目标总章数。
2. 根据现有章节文件判断下一章。
3. 读取当前章细纲；没有细纲时，根据配置决定是否从卷纲自动兜底。
4. 调用模型生成章节。
5. 执行硬伤门、跨章扫描、重复结构扫描、正式稿保存前检查。
6. 写入 `output/`。
7. 给本章打状态：
   - `正式可用`
   - `可继续但需复核`
   - `待审暂停`
8. 通过发布检查的章节复制到 `publish/`。
9. 更新记忆和日志。
10. 如果没有硬暂停问题，继续下一章，直到终章。

默认策略是宽松跑完。普通瑕疵只进日志，不阻断整本托管。

## 7. 必须暂停的情况

以下情况会暂停托管：

- API 余额不足、鉴权失败、网络或模型返回异常。
- 上下文超限。
- 空稿。
- 章节号错误。
- 严重重复结构。
- 命中硬禁词。
- 严格真相越界。
- 当前章跨章 HIGH。
- 正式章节断档。

## 8. 新书准备清单

开新书前至少准备：

- `project_config.json`：书名、总章数、卷范围、字数目标。
- `characters/`：主要角色设定。
- `world_building/`：世界观、规则、职业、地点、势力。
- `plot/全书大纲.txt`：全书主线。
- `plot/当前卷大纲.txt`：当前卷目标和阶段推进。
- `plot/第一卷逐章细纲.txt`：逐章细纲。
- `plot/唯一真相设定表.md`：悬念、真相、揭示层级。
- `plot/tool_rules.json`：禁词、旧名、跨章事件、门控。
- `plot/reveal_rules.json`：真相揭示规则。

建议流程：

1. 先填全书大纲。
2. 再填第一卷逐章细纲。
3. 再填角色和世界观。
4. 再跑“体检大纲”。
5. 体检通过后再托管。

## 9. 发布与分享规则

可以分享：

- 源码。
- `config.example.json`。
- `project_config.json` 的空白或示例配置。
- `plot/` 下的空白模板和规则模板。
- `templates/` 题材模板。
- `README.md`、`工具使用说明.md`、本交接文档。

不要分享：

- `config.json` 里的真实 API Key。
- `output/` 里的工作稿。
- `publish/` 里的正式正文，除非明确要发书。
- `logs/` 里的审稿日志。
- `history/` 里的上下文缓存。
- 个人未公开设定。

## 10. 维护建议

- 新题材优先新增 `templates/题材名/`，不要直接改代码。
- 新规则优先写入 `plot/tool_rules.json` 或 `plot/reveal_rules.json`。
- 新文风要求优先写入 `project_config.json` 的 `tone_rules`。
- 只有通用能力才改 `generator.py` 或 `gui_app.py`。
- 每次大改后至少做四项检查：
  - Python 编译检查。
  - JSON 校验。
  - 旧书专属词搜索。
  - GUI 启动和体检大纲试跑。

## 11. 当前状态

- 已完成通用化合并。
- 已上传 GitHub 主分支。
- 已保留旧 GitHub 主分支备份：`backup-main-before-novel-tool-20260518`。
- 当前分享包用于交接和离线分发。

